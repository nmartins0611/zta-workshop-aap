= Section 2 — Deploy application with short-lived credentials
:toc: left
:toclevels: 3
:icons: font
:idprefix:
:idseparator: -


[#lab-common-environment]
== Lab environment reference

These hostnames assume workshop DNS (`*.zta.lab`) is configured. Replace with instructor-provided values if different.

.Access information
[cols="2,2,3",options="header"]
|===
| System | URL / address | Credentials

| Automation controller (AAP 2.6)
| `https://control.zta.lab`
| As provided by instructor

| Event-Driven Ansible controller (AAP 2.6)
| Same platform as automation controller (EDA UI / route per deployment)
| As provided by instructor

| IdM (FreeIPA)
| `https://central.zta.lab`
| `admin` / `ansible123!`

| OPA
| `http://central.zta.lab:8181`
| No auth (lab)

| Vault
| `http://vault.zta.lab:8200`
| `admin` / `ansible123!` (or userpass as documented)

| NetBox
| `http://netbox.zta.lab:8880`
| API token as documented / instructor

| Gitea
| `http://gitea.zta.lab:3000`
| As provided by instructor

| Splunk
| `http://splunk.zta.lab:8000` or `http://central.zta.lab:8000`
| As configured during setup

| Wazuh Dashboard
| `http://wazuh.zta.lab:5601`
| As provided by instructor

| Application (Global Telemetry Platform)
| `http://app.zta.lab:8081`
| No auth (dashboard)
|===


== Overview

Deploy the **Global Telemetry Platform** using Vault dynamic database credentials. Experience OPA **deny** for the wrong user, then **allow** for `app-deployers`. Observe micro-segmentation (Arista) and credential TTL.

[IMPORTANT]
.Why this section exists
====
Deploying an app is where **identity (who)**, **policy (may they)**, **secrets (with what credential)**, and **network (on which path)** meet. In Zero Trust you **do not** hand every engineer a long-lived DB password or open the database to the whole VLAN. This pipeline shows **policy before secrets** and **least-privilege network paths** aligned with short-lived credentials—so compromise of one layer does not instantly mean full data access.
====

.Core vs extended
[cols="2,1,1",options="header"]
|===
| Exercise | Track | Est.
| 2.1–2.6 | Core | ~30 min (2-hour path)
| 2.7–2.8 | Extended | optional
| 2.9–2.11 | Extended | break/fix labs
|===

[TIP]
.Separation of concerns in templates
====
Each step has a single job: **decide** (OPA), **issue** (Vault), **enforce path** (Arista), **configure app**. That mirrors ZTA’s idea that **no single giant playbook** should skip gates—each stage can fail closed, be audited, and be replaced without collapsing the whole story.
====

== Exercise 2.1 — Workflow

As the ztuser, login to the automation platform and navigate to Automation Execution -> Templates and click on Create template to create and Workflow Template. 

Create the workflow with the following details:

Name: **Deploy Application Pipeline**:
inventory: ZTA Lab Inventory
Organization: Default

Click on Create workflow job template, this will take you to the Workflow Visualizer - We will now add templates. click on Add Step - select Job Template and select Check DB Access Policy, click Next and continue. 

Right click on the template in the visualiser by the kebab menu ⋮. - and select Add step and link - this time select the Create DB Credential template and add the status Run on success. Repeat the process to add tempaltes 
Configure DB Access List and Deploy Application. Once done, save the workflow.


[TIP]
.Order of operations = trust boundaries
====
**Check DB Access Policy runs first** on purpose: if the human (via AAP) is not entitled to database credentials, the workflow **never touches Vault**. That is *defence in depth*—the secret manager is not a substitute for authorisation. In breach terms, an attacker who tricks someone into clicking “deploy” still hits a **PDP (OPA)** before any **secret issuance**.
====

== Exercise 2.2 

. Log into AAP as `neteng` and navigate to the Templates section. Notice you have no permissions. Navigate to the IDM tab and login with the admin details.AAp is using the LDAP for authenticationa and mapping access for users based on their groups in the iDM. Search for neteng on active users. Select the account and click on User Groups - We can seee they are just part of the ipa user group so they have access but not much else. Lets add them to the ifnrastructure group. Click add search infrastructure, then select the group and click on the array to assign it. 

Navigate back to the controller. Log out and log back in as the neteng user. We now have access to the content the infrastructure team has access to. Navigate to Templates and execute the Depoly Application Pipeline workflow.

Expect first step to show OPA **DENIED**; workflow stops; Vault not contacted.


msg: |
  OPA Database Access Decision:
    User:            neteng
    Groups (IdM):  ipausers, team-infrastructure
      Database:        ztaapp
    Decision:        DENIED
    Reason:          OPA returned no decision document (check policy load and URL).
    
_ansible_verbose_always: true
_ansible_no_log: false
changed: false


Now logout and lets login in with the appdev user. Navigate to the same template and trigger it. Notice this user has been approved to trigger this automation. Click on the Check DB policy now to see the details. 


Now logout and lets login in with the appdev user. Navigate to the same template and trigger it. Notice this user has been approved to trigger this automation. Click on the Check DB policy now to see the details. 


TASK [Display OPA decision] ****************************************************
40
ok: [central] => {
41
"msg": "OPA Database Access Decision:\n User: appdev\n Groups (IdM): team-applications, app-deployers, ipausers\n Database: ztaapp\n Decision: ALLOWED\n Reason: all conditions met — credential issuance approved\n \n"
42
}
43
Monday 13 April 2026 19:58:52 +0000 (0:00:00.042) 0:00:02.446 **********
44
45
TASK [Enforce OPA decision] ****************************************************
46
ok: [central] => {
47
"changed": false,
48
"msg": "OPA policy check passed — proceeding with credential issuance"
49
}
50

Go back to the workflow and as it should have completed now. 

navigate to the Terminal and do the following curl 


[bash]
----
curl -w "\n\nConnect: %{time_connect}s\nTLS: %{time_appconnect}s\nTotal: %{time_total}s\nHTTP: %{http_code}\n" \
  -so /dev/null http://app.zta.lab:8081/health




Connect: 0.002681s
TLS: 0.000000s
Total: 0.010493s
HTTP: 200

----

We can see the application is healthy with its new time limited credentials (Status 200). 
Navigate to the Telemetry tab and view the application is still working. Now wait a few mins and run the same command:

[bash]
----
[rhel@central ~]$ curl -w "\n\nConnect: %{time_connect}s\nTLS: %{time_appconnect}s\nTotal: %{time_total}s\nHTTP: %{http_code}\n" \
  -so /dev/null http://app.zta.lab:8081/health


Connect: 0.001888s
TLS: 0.000000s
Total: 0.007571s
HTTP: 503
----

The application is no longer healthy with a 503 status. The time limited credentials have now expired.









[TIP]
.What the learner should feel here
====
The unauthorised user **never sees** a database password. Zero Trust is not only about encryption—it is about **shrinking who can cause sensitive actions**. Deny-by-default policy turns “I have network reachability” into “I still cannot obtain credentials.”
====

== Exercise 2.4 — Policy In action.

`zta.db_access` requires `app-deployers`, valid DB (`ztaapp`), and bounded permissions.

*Fix:* log in as `appdev`, or run `ipa group-add-member app-deployers --users=neteng` on central.

[TIP]
.Identity-driven access
====
**IdM groups** are attributes the PDP understands. Changing group membership is how the organisation **grants or revokes** deployment authority without editing playbooks. That is **policy + identity lifecycle** working together—important when people change roles or leave.
====

== Exercise 2.5 — Correct user (allowed)

As `appdev`, launch the workflow. Expect OPA allow, Vault dynamic user, ACL update, app healthy at `http://app.zta.lab:8081`.

[TIP]
.Micro-segmentation after authorisation
====
Even with valid DB credentials, **network policy** still limits *where* connections may originate. The Arista step models **segmentation** that is common in ZTA: secrets prove *who the app is*, ACLs constrain *which paths are valid*. An attacker with stolen creds from a random laptop should still fail if the fabric only permits the app tier IP.
====

== Exercise 2.6 — Credential expiry

After deploy, confirm Vault user in PostgreSQL; wait TTL (e.g. 5 minutes); user disappears; `curl http://app.zta.lab:8081/health` reflects loss of DB access until rotation/restore.

[TIP]
.Ephemeral credentials change operations
====
TTL expiry is a feature, not a bug: it forces **rotation**, **re-authorisation**, or **deliberate renewal** instead of silent, multi-year database passwords. In ZTA, **standing access** to data is the exception and must be justified; here the app must be **re-fed** credentials on a schedule or on demand—exactly what `Rotate DB Credentials` (extended) practices.
====

== Validation checklist

* [ ] `neteng` denied at policy step
* [ ] `appdev` full success
* [ ] Unique Vault DB user per run
* [ ] App reachable at :8081
* [ ] TTL removes user; app unhealthy without fresh creds

== Extended exercises

include::includes/extended-exercises.adoc[]
