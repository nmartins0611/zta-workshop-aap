= Section 1 — Verify ZTA Components and AAP Integration
:toc: left
:toclevels: 3
:icons: font
:experimental:
:idprefix:
:idseparator: -

This lab mirrors `section1/README.md` in AsciiDoc. Use it for student handouts or PDF/HTML builds. UI steps use the **automation controller** in **Ansible Automation Platform 2.6** (see shared platform note below).

[NOTE]
.How to read these labs (Zero Trust)
====
In NIST SP 800-207 terms, **Ansible Automation Platform (AAP)** is the *Policy Enforcement Point (PEP)*: operational change flows *through* it. **Open Policy Agent (OPA)** is a *Policy Decision Point (PDP)*: it answers allow/deny from **identity** (IdM), **state** (NetBox), and **context** encoded in Rego. **Vault** supplies *short-lived, least-privilege* secrets instead of long-lived passwords in CMDBs or disks. **SPIFFE/SPIRE** (later sections) binds actions to a *verified workload*, not only a human username.

**Why that matters:** Zero Trust assumes breach and removes implicit trust. You will repeatedly see *verify then act*, *deny by default*, *least privilege*, and *auditability*—not as buzzwords but as concrete gates in the automation path.
====

[NOTE]
.Platform version (Red Hat Ansible Automation Platform 2.6)
====
These labs target **Red Hat Ansible Automation Platform 2.6**: the **automation controller** (job and workflow templates, credentials, inventories, projects) and the **Event-Driven Ansible controller** (rulebook activations, decision environments, projects, credentials). See the https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/2.6/[AAP 2.6 documentation] and https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/2.6/html/using_automation_decisions/[Using automation decisions] for EDA.

Menu groupings vary by **role and organization** (for example **Access** vs **Automation Execution**). Steps that mention *Resources → Credentials* mean **Credentials** in the automation controller; use **Projects**, **Inventories**, **Templates**, and **Workflows** in the same UI. EDA steps use the **Event-Driven Ansible controller** (rulebook activations, decision environments).
====

[#lab-common-environment]
== Lab environment reference

These hostnames assume workshop DNS (`*.zta.lab`) is configured. Replace with instructor-provided values if different.

.Access information
[cols="2,2,3",options="header"]
|===
| System | URL / address | Credentials

| Automation controller (AAP 2.6)
| `https://control.zta.lab`
| As provided by instructor

| Event-Driven Ansible controller (AAP 2.6)
| Same platform as automation controller (EDA UI / route per deployment)
| As provided by instructor

| IdM (FreeIPA)
| `https://central.zta.lab`
| `admin` / `ansible123!`

| OPA
| `http://central.zta.lab:8181`
| No auth (lab)

| Vault
| `http://vault.zta.lab:8200`
| `admin` / `ansible123!` (or userpass as documented)

| NetBox
| `http://netbox.zta.lab:8880`
| API token as documented / instructor

| Gitea
| `http://gitea.zta.lab:3000`
| As provided by instructor

| Splunk
| `http://splunk.zta.lab:8000` or `http://central.zta.lab:8000`
| As configured during setup

| Wazuh Dashboard
| `http://wazuh.zta.lab:5601`
| As provided by instructor

| Application (Global Telemetry Platform)
| `http://app.zta.lab:8081`
| No auth (dashboard)
|===

.Primary scenario accounts (IdM)
[cols="2,2,3",options="header"]
|===
| Username | Groups | Role in exercises

| `ztauser`
| `zta-admins`, `patch-admins`, `app-deployers`
| General admin — can patch and deploy

| `netadmin`
| `zta-admins`, `network-admins`
| Network admin — VLAN exercises

| `appdev`
| `app-deployers`
| App developer — deployment pipeline

| `neteng`
| _(none)_
| Often denied (wrong user scenarios)
|===

All IdM user passwords for workshop accounts: `ansible123!`

For full org charts (infrastructure, DevOps, security, applications teams) see `docs/exercise-guide.md` or the repository root `README.md`.

[#lab-aap-workshop-defaults]
=== Automation controller — workshop defaults (Section 1)

When the instructor runs `setup/configure-aap-project.yml` and `setup/configure-aap-inventory.yml`, these names are typical. Use them in labs unless your environment differs.

[cols="2,3",options="header"]
|===
| Resource | Default name / note
| Organization | `Default`
| Execution environment | `ZTA Workshop EE` (custom image from project setup; includes Arista/RHEL-oriented collections)
| Project | `ZTA Workshop` (Git URL to Gitea workshop repo)
| Inventory | `ZTA Lab Inventory` (static from setup, or NetBox-sourced per lab)
| Job templates (Section 1) | Verify ZTA Services; Test Vault Integration; Test Vault SSH Certificates; Test OPA Policy
|===

**Credentials on jobs (Section 1):** attach **ZTA Machine Credential** to all four templates; add **ZTA Arista Credential** only to **Verify ZTA Services** (EOS facts play). **ZTA Vault Credential** is never attached directly—it backs Machine/Network lookups. **ZTA Gitea Credential** is for **project** sync only.

== Overview

Connect Ansible Automation Platform to the Zero Trust stack. Workshop credentials are pre-created with `setup/configure-aap-credentials.yml`; you verify Vault lookups and related objects, then configure inventory, project, templates, and run checks against IdM, Vault, OPA, NetBox, and Gitea.

[IMPORTANT]
.Why this section exists
====
Until AAP is wired to the same **identity, policy, secrets, and inventory truth** the rest of the architecture uses, automation would be a *shadow admin path*—easy to bypass Zero Trust intent. Here you make AAP a **first-class citizen** of the ZTA fabric: it authenticates like other services, reads policy from OPA, pulls secrets from Vault, and builds inventory from NetBox so *what you run jobs against* matches *what the organisation says exists*.
====

.Workshop delivery (2-hour track)
[cols="3,1,1",options="header"]
|===
| Exercise | Track | Est. time

| Prerequisite — LDAP + Policy as Code (verify) | Core | 2 min
| 1.1 — Verify AAP credentials (Vault lookups) | Core | 5 min
| 1.2 — IdM LDAP (Authentication Methods UI) | Core | 5 min
| 1.3 — Inventory and project | Core | 5 min
| 1.4 — Job templates | Core | 3 min
| 1.5 — Verify ZTA services | Core | 2 min
| 1.6 — Vault dynamic credentials | Core | 3 min
| 1.7 — Vault SSH signed certificates | Core | 3 min
| 1.8 — OPA policy decisions | Core | 2 min
| 1.9 — Certificate trust debugging | Extended | 25 min
|===

For a short workshop, complete **1.1–1.8** (~25–30 min). Skip **1.9** unless you have extra time. Confirm the **LDAP + Policy as Code** prerequisite below first (often already applied during lab deployment).

== Controller: IdM LDAP and Policy as Code (prerequisite)

Later sections assume the automation controller accepts **IdM users** for login and enforces **Policy as Code** (OPA gateway) before jobs run. In a full lab build this is usually applied **during deployment** (before Section 1), not in the UI during class.

[TIP]
.Typical automation order
====
`docs/deployment-guide.md` **Layer 7 — Policy** documents the playbooks:

* `setup/configure-opa-base.yml` — load OPA policies (including `aap.gateway`).
* `setup/configure-aap-policy.yml` — enable controller ↔ OPA (Policy as Code).
* `setup/configure-aap-ldap.yml` — controller **LDAP authentication** against IdM (LDAPS).

Many instructors run **LDAP earlier** in setup (still Layer 7); that is fine. Section 1 asks you to **confirm** both integrations so exercises 2–4 behave as written.
====

=== LDAP (controller → IdM)

* **What it does:** Users log into `https://control.zta.lab` with **workshop IdM accounts** (for example `ztauser` / `ansible123!`), not only the platform `admin` account.
* **Prerequisites:** IdM running; controller trusts the IdM CA (for example after `setup/enroll-idm-clients.yml`). Details: `HANDOFF.md` (*LDAP: controller → IdM*).

=== Verify LDAP

. Open the automation controller.
. Sign in with an **IdM user** (for example `ztauser`, password `ansible123!` per link:../../docs/lab/common-environment.adoc#lab-common-environment[Lab environment reference]).
. If login fails: complete **Exercise 1.2** (Authentication Methods UI), or confirm `setup/configure-aap-ldap.yml` (legacy API) completed, DNS reaches IdM, and troubleshooting in `docs/deployment-guide.md` / `HANDOFF.md`.

=== Policy as Code (controller → OPA)

* **What it does:** Before starting a job, the controller can query OPA at `v1/data/aap/gateway/decision` (gateway Rego). Section 3 and 4 depend on this **outer ring**.
* **Automation:** `setup/configure-aap-policy.yml` after `setup/configure-opa-base.yml`.

=== Verify Policy as Code

. As platform **admin**, open **Settings** → **Policy** / **Open Policy Agent** / **Policy as Code** (exact label varies by patch) and confirm OPA URL, policy path, and enable flags match `docs/deployment-guide.md` Layer 7 (*Manual Policy as Code (OPA gateway)*).
. Or use the API:

[source,bash]
----
curl -sk -u "admin:ansible123!" \
  "https://control.zta.lab/api/controller/v2/settings/opa/" | python3 -m json.tool
----

Expect `OPA_ENABLED` and `OPA_PRE_ACTION_ENABLED` true; `OPA_POLICY_PATH` is `v1/data/aap/gateway/decision`; `OPA_URL` matches your lab OPA (typically `http://central.zta.lab:8181` per `inventory/group_vars/all.yml`).

If Policy as Code was not applied by automation, use the **manual UI or API** steps in `docs/deployment-guide.md` Layer 7 or run `setup/configure-aap-policy.yml`.

== Exercise 1.1 — Verify AAP credentials (pre-configured, Vault-sourced)

=== Goal

Workshop credentials are created with `setup/configure-aap-credentials.yml`. You **confirm** Vault KV data and controller objects match the lab—no manual credential creation in the UI for this exercise.

[TIP]
.Zero Trust: secrets on the platform
====
Machine and Arista passwords are **not** stored in AAP: the controller holds **Vault paths** and resolves them at job time. Only the bootstrap Vault lookup (and NetBox token) behave like classic "stored" secrets in the platform database—tighten that in production (AppRole, TLS, policy).
====

=== Step 1 — Confirm Vault paths

[source,bash]
----
export VAULT_ADDR=http://vault.zta.lab:8200
vault login -method=userpass username=admin password=ansible123!

vault kv get secret/machine/rhel
vault kv get secret/network/arista
vault kv get secret/db/admin
----

=== Step 2 — Confirm credentials in the controller

Open **Credentials** and verify these names exist (from `setup/configure-aap-credentials.yml`). Edit each and confirm:

* `ZTA Vault Credential` — Vault URL and v2; bootstrap auth present.
* `ZTA Machine Credential` — user `rhel`, sudo; **Password** and **Become** use lookups to `secret/data/machine/rhel` (`password`, `become_password`).
* `ZTA Arista Credential` — lookups on `secret/data/network/arista`.
* `ZTA NetBox Credential` — URL and token match NetBox.
* `ZTA Vault SSH Credential` — present (AppRole / signed SSH).

Do not change lookup paths unless an instructor is troubleshooting.

[TIP]
.Gitea credential
====
`configure-aap-credentials.yml` does **not** create **ZTA Gitea Credential**. Confirm or add it when you set up the **ZTA Workshop** project (Exercise 1.3) if Git requires it.
====

Functional checks: **Exercise 1.5** (Verify ZTA Services) and **1.6–1.7** prove lookups from AAP.

=== Step 3 — NetBox type missing?

If the **NetBox** credential type or **ZTA NetBox Credential** is absent, follow the **Manual: NetBox credential type** steps in `section1/README.md`, then re-run automation or recreate the NetBox credential per Exercise 1.3.

=== Step 4 — Vault SSH CA

[source,bash]
----
vault read ssh/config/ca
vault read ssh/roles/ssh-signer
----

.Credential summary
[cols="2,2,3",options="header"]
|===
| Credential | Type | Source
| ZTA Vault Credential | Vault lookup | Bootstrap (stored in AAP)
| ZTA Machine | Machine | KV `secret/data/machine/rhel` at job time
| ZTA Arista | Network | KV `secret/data/network/arista` at job time
| ZTA NetBox | NetBox | Token in AAP
| ZTA Vault SSH | Vault Signed SSH | AppRole + `ssh-signer`
|===

[TIP]
.Vault SSH CA vs static keys
====
**Signed SSH certificates** are the Zero Trust answer to shared static keys. Exercise 1.7 exercises this path end-to-end.
====

[[exercise-1-2-idm-ldap-authentication-methods]]
== Exercise 1.2 — IdM LDAP (Authentication Methods UI)

=== Goal

On **Ansible Automation Platform 2.6**, LDAP against IdM is often configured under **Access Management** → **Authentication Methods** → **Create authentication** (not only the legacy `PATCH /api/controller/v2/settings/ldap/` path used by `setup/configure-aap-ldap.yml`). This exercise records the **IdM field values** that match the workshop playbooks and `inventory/group_vars/all.yml`.

If IdM login already works, you can **skim** this exercise to validate settings; if logins fail on gateway-based installs, complete it.

=== Prerequisites

* IdM (FreeIPA) on `central.zta.lab` with domain `zta.lab` → base DN `dc=zta,dc=lab`.
* Controller **trusts the IdM CA** for LDAPS (lab: `setup/enroll-idm-clients.yml` on the controller host).
* IdM **`admin`** password: workshop default `ansible123!` (same as `idm_admin_password`).

Adjust hostnames and `dc=` components if your lab uses a different `idm_domain`.

=== Steps — Create LDAP authentication

. Log in as platform **admin**. Open **Access Management** → **Authentication Methods** → **Create authentication**.
. Set **Authentication type** to **LDAP**.
. Fill the top fields:

+
[cols="2,3",options="header"]
|===
| Field | Value (workshop defaults)
| Name | `IdM LDAP` (or any label)
| LDAP Server URI | `ldaps://central.zta.lab`
| LDAP Bind DN | `uid=admin,cn=users,cn=accounts,dc=zta,dc=lab`
| LDAP Bind Password | IdM `admin` password (`ansible123!` unless overridden)
| LDAP Group Type | `GroupOfNamesType` (recommended) or `MemberDNGroupType` — see parameters below
| LDAP User DN Template | Leave **empty**
| LDAP Start TLS | **Off** (LDAPS already encrypts the connection)
|===

. **LDAP Connection Options** (YAML):

+
[source,yaml]
----
OPT_REFERRALS: 0
OPT_NETWORK_TIMEOUT: 30
----

. **LDAP Group Type Parameters** (required). These are the **key/value arguments for the selected group type's constructor** (`LDAP_GROUP_TYPE_PARAMS`). Invalid keys for that type produce *Invalid option for specified GROUP_TYPE*.

+
Per https://django-auth-ldap.readthedocs.io/en/latest/reference.html#django_auth_ldap.config.LDAPGroupType and the LDAP group-type table in https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/2.5/html/access_management_and_authentication/gw-configure-authentication#controller-set-up-LDAP[Red Hat AAP 2.5 Access management] (same idea in 2.6):

+
* **GroupOfNamesType** — IdM `groupOfNames`; equivalent to `MemberDNGroupType('member')`. Only **`name_attr`** is a constructor argument (`member` is fixed):

+
[source,yaml]
----
name_attr: cn
----

+
* **MemberDNGroupType** — generic type; constructor takes **`member_attr`** and **`name_attr`**. Use this if you want both fields explicitly (matches the doc example `{"name_attr": "cn", "member_attr": "member"}`):

+
[source,yaml]
----
name_attr: cn
member_attr: member
----

+
For FreeIPA/IdM **groupOfNames**, the two options above are **functionally equivalent**.

+
[TIP]
.MemberDNGroupType vs GroupOfNamesType
====
**GroupOfNamesType** + `name_attr` only is the shortest correct form. **MemberDNGroupType** + `name_attr` and `member_attr` is equally valid for IdM when `member_attr` is `member`. Do not pass `member_attr` when **LDAP Group Type** is **GroupOfNamesType**—that type does not accept it.
====

. **LDAP User Search** (YAML list — three elements):

+
[source,yaml]
----
- cn=users,cn=accounts,dc=zta,dc=lab
- SCOPE_SUBTREE
- (uid=%(user)s)
----

. **LDAP Group Search** (YAML list — three elements):

+
[source,yaml]
----
- cn=groups,cn=accounts,dc=zta,dc=lab
- SCOPE_SUBTREE
- (objectClass=groupOfNames)
----

. **LDAP User Attribute Map** (required; YAML):

+
[source,yaml]
----
first_name: givenName
last_name: sn
email: mail
----

. Enable **Enabled** (and **Create objects** if you want local user records on first IdM login). Save (**Create Authentication Method**).

=== Organisation and team mapping

`setup/configure-aap-ldap.yml` also sets **organisation map**, **team map**, and **superuser group** (`zta-admins`, `team-infrastructure`, etc.). Those may appear as **separate authenticator maps** (or advanced fields) in Access Management. If IdM users log in but lack roles or organisations, mirror the `ldap_config` block in `setup/configure-aap-ldap.yml` using the https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/2.6/html/access_management_and_authentication/[AAP 2.6 Access management and authentication] guide.

==== Why authenticator mapping matters for Zero Trust

In a Zero Trust model, **authentication alone is insufficient**. Proving identity (LDAP bind succeeds) does not grant any permissions — that is by design. Authenticator maps are the mechanism that translates a verified external identity into scoped, least-privilege access within AAP:

* **Without maps:** An IdM user logs in successfully but sees an empty platform — no organisation membership, no teams, no templates. Authentication happened, but authorisation did not.
* **With maps:** LDAP group membership (e.g. `team-security`) flows into an AAP team assignment, which in turn controls which job templates that user can see and execute. The external identity provider (IdM) remains the **single source of truth** — no parallel local accounts to maintain, and offboarding in IdM immediately removes AAP access on next login.
* **Least privilege by construction:** Each map grants the minimum role needed. Organisation members can view; team members can execute their team's templates; only `zta-admins` receive org-admin or superuser. There is no "everyone gets everything" default.
* **Auditability:** Every job execution traces back through the map chain: IdM user → LDAP group → AAP team → template permission → job run. This is the audit trail Zero Trust demands.

This is why Exercise 1.2 is a **prerequisite for Sections 2–4** — without working maps, IdM users cannot reach the policy-gated templates those sections depend on.

==== Which groups you reference (by DN)

These are the IdM groups the workshop uses in **Group** triggers (`dc=zta,dc=lab`). Use the **full DN** in the **Groups** list for each map (one DN per entry; if you combine several groups in one rule, include each DN the UI expects for that operation).

[cols="2,4",options="header"]
|===
| IdM group (`cn`) | Full DN (paste into **Groups**)
| `zta-admins` | `cn=zta-admins,cn=groups,cn=accounts,dc=zta,dc=lab`
| `team-infrastructure` | `cn=team-infrastructure,cn=groups,cn=accounts,dc=zta,dc=lab`
| `team-devops` | `cn=team-devops,cn=groups,cn=accounts,dc=zta,dc=lab`
| `team-security` | `cn=team-security,cn=groups,cn=accounts,dc=zta,dc=lab`
| `team-applications` | `cn=team-applications,cn=groups,cn=accounts,dc=zta,dc=lab`
|===

[NOTE]
.Login vs role grants
====
You are **not** "allowing" only these users to log in **unless** you add a separate **Allow** map that lists those DNs. **Organization**, **Team**, **Superuser**, and similar maps mean: *if* the authenticated user is in that LDAP group, *then* grant the mapped organisation role, team membership, or superuser flag. Everyone else can still log in if LDAP authentication itself permits them; they simply will not receive those grants without a matching map.

For **Allow** maps and other trigger types, see https://docs.redhat.com/en/documentation/red_hat_ansible_automation_platform/2.6/html/access_management_and_authentication/gw-configure-authentication#gw-authenticator-map-types[Authenticator map types] in the AAP 2.6 Access management guide.
====

==== Suggested maps (UI)

Use **Add authentication mapping** on your LDAP authenticator. Create these maps in order:

. **Organisation → Member (Always)** — ensures every LDAP-authenticated user becomes a member of the **Default** organisation. Without this map, users log in but see an empty platform with no resources.

+
[cols="2,3",options="header"]
|===
| Field | Value
| Map type | Organisation
| Organisation | Default
| Role | Member
| Trigger | Always
|===

. **Organisation → Admin (Group)** — grants organisation admin to `zta-admins`:

+
[cols="2,3",options="header"]
|===
| Field | Value
| Map type | Organisation
| Organisation | Default
| Role | Organization Admin
| Trigger | Group
| Groups | `cn=zta-admins,cn=groups,cn=accounts,dc=zta,dc=lab`
|===

. **Superuser (Group)** — (**optional**; use only if IdM `zta-admins` should equal full platform admin):

+
[cols="2,3",options="header"]
|===
| Field | Value
| Map type | Superuser
| Trigger | Group
| Groups | `cn=zta-admins,cn=groups,cn=accounts,dc=zta,dc=lab`
|===

. **Team (Group)** — one map per team, each mapping an IdM group to an AAP team in **Default**:

+
[cols="2,2",options="header"]
|===
| IdM group (`cn`) | AAP team name (Default org)
| `team-infrastructure` | Infrastructure
| `team-devops` | DevOps
| `team-security` | Security
| `team-applications` | Applications
|===

+
For each row, create a map with **Map type** = Team, **Team** = the AAP team name, **Organisation** = Default, **Trigger** = Group, and paste the **full DN** into the **Groups** field.

=== Verify

. Open an incognito window (or sign out). Log into `https://control.zta.lab` as **`ztauser`** / `ansible123!` (see link:../../docs/lab/common-environment.adoc#lab-common-environment[Lab environment reference]).
. Confirm the user lands on a populated dashboard (not an empty platform).
. Check that team membership is visible under **Access Management** → **Users** → `ztauser` → **Teams**.

== Exercise 1.3 — Inventory and project

[TIP]
.Source of truth and GitOps
====
**NetBox** is the *infrastructure source of truth* in this workshop: if a host is decommissioned there, it should disappear from automation inventory on sync—reducing "ghost" targets attackers or operators could hit. **Gitea** holds *intent* (playbooks); together they separate **what exists** (CMDB) from **what we want to do** (code), both subject to policy in later sections.
====

=== Project (Gitea)

**Projects** → **Add**:

* Name: `ZTA Workshop`
* Git URL: `http://gitea.zta.lab:3000/zta-workshop/zta-app.git`
* Credential: `ZTA Gitea Credential`
* Save; wait for successful sync.

=== Inventory (NetBox)

. **Inventories** → create `ZTA Lab Inventory`.
. Open the inventory → **Sources** → **Add**:

+
[cols="2,3",options="header"]
|===
| Field | Value
| Name | NetBox CMDB
| Source | Sourced from a project
| Project | ZTA Workshop
| Inventory file | `inventory/netbox_inventory.yml`
| Credential | ZTA NetBox Credential
|===

. Enable **Overwrite** and **Update on launch**; sync and verify hosts (central, control, vault, netbox, app, db, ceos1–3, etc.).

[TIP]
.Update on launch
====
Refreshing inventory **before each job** reduces stale targeting: policy and playbooks see **current** CMDB state, which is how Zero Trust ties **continuous authorisation** to **current asset posture** (at least at inventory granularity).
====

[[exercise-1-4-job-templates]]
== Exercise 1.4 — Job templates

[TIP]
.Why these four templates
====
You are building an **evidence trail** that each pillar works before you trust it for real change. In ZTA terms this is **verify, then enforce**: IdM/Vault/OPA/network paths must be healthy or later sections will fail in ambiguous ways. Treat these jobs as **control-plane health checks** for the PEP (AAP).
====

Create templates using **ZTA Lab Inventory** and **ZTA Machine Credential**:

[cols="2,3",options="header"]
|===
| Name | Playbook
| Verify ZTA Services | `section1/playbooks/verify-zta-services.yml`
| Test Vault Integration | `section1/playbooks/test-vault-integration.yml`
| Test Vault SSH Certificates | `section1/playbooks/test-vault-ssh.yml`
| Test OPA Policy | `section1/playbooks/test-opa-policy.yml`
|===

**Credentials on jobs (Section 1):** attach **ZTA Machine Credential** to all four templates; add **ZTA Arista Credential** only to **Verify ZTA Services** (EOS facts play). **ZTA Vault Credential** is never attached directly—it backs Machine/Network lookups. **ZTA Gitea Credential** is for **project** sync only.

== Exercise 1.5 — Verify ZTA services

Run **Verify ZTA Services**. Expect healthy IdM, Vault, OPA, NetBox, DNS, PostgreSQL, and Arista facts.

[TIP]
.Never trust, always verify
====
**Service checks** are the operational face of "never trust, always verify": you do not assume IdM or OPA is up because yesterday it was. For the workshop, this mirrors **continuous diagnostics** on which policy decisions and secret issuance depend.
====

== Exercise 1.6 — Test Vault integration

Run **Test Vault Integration**. Observe short-lived PostgreSQL users (unique name per run, TTL, revoke at end).

[TIP]
.Short-lived database identities
====
Dynamic DB users with a **TTL** limit how long a leaked credential is useful—core **assume breach** hygiene. Vault **revokes** the lease so the database role disappears without a human running `DROP USER`. That is how **least privilege** becomes **time-bound privilege**.
====

== Exercise 1.7 — Test Vault SSH certificates

Run **Test Vault SSH Certificates**. Observe CA-signed cert, TTL, principals, successful SSH to app/db.

Optional manual sign (from README):

[source,bash]
----
export VAULT_ADDR=http://vault.zta.lab:8200
vault login -method=userpass username=admin password=ansible123!
ssh-keygen -t rsa -b 2048 -f /tmp/ephemeral -N '' -q
vault write -field=signed_key ssh/sign/ssh-signer \
  public_key=@/tmp/ephemeral.pub valid_principals=rhel > /tmp/ephemeral-cert.pub
ssh-keygen -L -f /tmp/ephemeral-cert.pub
----

[TIP]
.Cryptographic proof of permission to log in
====
The certificate encodes **who may act as which principal** and **for how long**. Hosts need only trust the CA once; they do not need network reachability to Vault at login time. That separation is useful in segmented networks and matches the ZTA idea that **access is explicitly granted and expires**.
====

== Exercise 1.8 — Test OPA policy

Run **Test OPA Policy**. Expect allow/deny scenarios for patching, VLAN, and DB access per group membership.

[TIP]
.Deny by default (PDP)
====
OPA returns **allow** only when Rego rules fire; everything else is **implicit deny**. **IdM groups** supply *role-like* attributes the PDP can use, but unlike a static RBAC matrix in one app, the same policy can be **versioned, tested, and shared** across AAP and other PEPs. You are seeing **central policy** backing **distributed enforcement**.
====

== Validation checklist (core)

* [ ] **LDAP:** IdM user (e.g. `ztauser`) can log into the automation controller; use **Exercise 1.2** (Authentication Methods UI) or `setup/configure-aap-ldap.yml` / `HANDOFF.md` as needed
* [ ] **Policy as Code:** OPA settings enabled and `OPA_POLICY_PATH` / `OPA_URL` correct (`setup/configure-aap-policy.yml` or manual Layer 7 steps in `docs/deployment-guide.md`)
* [ ] Vault KV paths present; controller shows ZTA Vault / Machine / Arista / NetBox / Vault SSH credentials with correct lookups
* [ ] Vault SSH credential present (from `configure-aap-credentials.yml`)
* [ ] NetBox inventory sync shows all lab hosts
* [ ] Gitea project synced
* [ ] Four verification templates run successfully

== Extended exercises

include::includes/extended-exercise-1-8.adoc[]
